<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- New separate action: keep Odoo defaults untouched -->
        <record id="action_account_invoice_merged_pdf" model="ir.actions.report">
            <field name="name">Invoices</field>
            <field name="model">account.move</field>
            <field name="report_type">qweb-pdf</field>
            <field name="report_name">account.report_invoice</field>
            <field name="report_file">account.report_invoice</field>
            <field name="is_invoice_report">True</field>
            <field name="print_report_name">(object._get_report_base_filename())</field>
            <field name="binding_model_id" ref="account.model_account_move"/>
            <field name="binding_type">report</field>
            <field name="domain" eval="[('move_type', 'in', ('out_invoice', 'out_refund'))]"/>
            <field name="groups_id" eval="[(4, ref('account.group_account_invoice')), (4, ref('account.group_account_readonly'))]"/>
        </record>
    </data>
</odoo>
